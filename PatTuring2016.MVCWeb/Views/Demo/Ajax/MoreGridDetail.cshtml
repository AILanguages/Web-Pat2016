@model PatTuring2016.MVCWeb.Models.ModelDetails

@Ajax.ActionLink(@Model.Title, "MoreGridDetail", new { Type = @Model.ListNumber, @Model.Part }, new AjaxOptions
                    {
                        UpdateTargetId = "console",
                        InsertionMode = InsertionMode.Replace,
                        HttpMethod = "GET"
                    }
                        )
